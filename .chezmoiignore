# Gemini CLI — credentials and runtime state. Never track these.
# (settings.json and GEMINI.md ARE tracked, under dot_gemini/)
.gemini/oauth_creds.json
.gemini/google_accounts.json
.gemini/google_account_id
.gemini/user_id
.gemini/installation_id
.gemini/mcp-oauth-tokens.json
.gemini/projects.json
.gemini/history
.gemini/tmp

# Self-updating trees and app-written state. These drift by design and made
# chezmoi-update fail on every run (prompt EOF under --no-tty); unmanaged
# 2026-07-29. oh-my-zsh updates itself in place; atuin has its own sync.
.oh-my-zsh
.local/share/atuin
.config/fish/fish_variables
.config/nvim/lazy-lock.json

# Repo documentation, not a dotfile to deploy into $HOME.
README.md
